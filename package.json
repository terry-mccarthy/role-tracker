{
  "name": "job-pipeline",
  "version": "1.0.0",
  "description": "Personal job search CRM with AI-powered role scoring",
  "private": true,
  "scripts": {
    "serve": "node --env-file=.env src/server/server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "better-sqlite3": "^12.9.0"
  }
}
